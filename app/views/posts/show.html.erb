<h1><%= @post.title %></h1>

<p><%= @post.message %></p>

<ul>
  <li><%= link_to "Edit", edit_post_path(@post) %></li>
  <li>
  <%= link_to "delete", @post, method: :delete,
                                data: { confirm: "Are you sure?" } %>
  </li>
</ul>

