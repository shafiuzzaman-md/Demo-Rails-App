<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
     <li>
        <%= link_to user.name, user %> &emsp; <%= link_to user.email, user %>
    </li>
  <% end %>
</ul>

<%= link_to "Create New User", new_user_path %>